version: '3.8'

services:
  backend-api:
    build: .
    container_name: fashion_recommender_api
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    env_file:
      - .env
    restart: unless-stopped